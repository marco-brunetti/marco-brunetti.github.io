/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
    "./*.{ts,tsx}",
    "*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        // Improved gray palette with better contrast
        gray: {
          lightest: "#ffffff", // Pure white for maximum contrast
          light: "#f1f3f4", // Very light gray for secondary text
          medium: "#9aa0a6", // Medium gray for muted elements
          dark: "#3c4043", // Dark gray for backgrounds
          darkest: "#202124", // Very dark gray/almost black for main background
        },
        // Portfolio theme with enhanced contrast
        hugo: {
          bg: "#202124", // Very dark background
          darker: "#171717", // Even darker for sections
          lighter: "#1a1a1a", // Really dark gray for cards with better contrast
          accent: "#8ab4f8", // Light blue-gray for better visibility
          text: "#ffffff", // Pure white for maximum text contrast
          muted: "#bdc1c6", // Lighter muted text for better readability
          border: "#5f6368", // More visible border color
          "button-primary": "#4285f4", // Clear primary button color
          "button-secondary": "#3c4043", // Secondary button background
          "button-hover": "#1a73e8", // Primary button hover
          "button-secondary-hover": "#5f6368", // Secondary button hover
        },
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: 0 },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: 0 },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
}
